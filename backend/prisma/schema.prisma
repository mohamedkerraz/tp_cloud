datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Number {
  id     Int    @id @default(autoincrement())
  number Int
}


